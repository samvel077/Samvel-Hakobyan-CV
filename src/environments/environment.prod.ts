export const environment = {
  // This is for production "baseHref" in angular.json "architect" -> "configurations" -> "production".
  // If you don't add the "baseHref in angular.json you also can use the cli command --base-href /your-href/
  // the baseHref must be between / / this lines
  BASE_HREF: '/Samvel-Hakobyan-CV',
  BASE_URL: 'https://samvel077.github.io/Samvel-Hakobyan-CV',
  PATH: '/Samvel-Hakobyan-CV',
  production: true,
};
